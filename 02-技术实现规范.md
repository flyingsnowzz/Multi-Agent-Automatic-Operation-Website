# 技术实现规范

**版本**: v2.0 | **更新**: 2026-06-29

## 运行命令

```bash
# 生产：全自动
python3 scripts/run_scheduler.py --source db --publish

# 生产：手动跑一次
python3 scripts/run_production.py --source db

# 测试：定时批量
python3 scripts/run_test_schedule.py

# 测试：单元
python -m pytest
```

## 调度逻辑

```
有文章 → 冷却 60s → 立刻下一轮
无文章 → 2h → 4h → 8h → 24h 退避
有文章时重置退避
```

## 评分体系

| 层 | 模型 | 阈值 | 方法 |
|---|---|---|---|
| AI 评分 | deepseek-chat | 80 | 百分位法 9 级锚点 |
| Quality | deepseek-chat | 70 | 百分位法 5 维评分 |
| 改写复评 | deepseek-chat | 75 | 写作质量复评 |

## 环境变量

```
# 必需
DEEPSEEK_API_KEY     # AI 评分、改写、SEO
QUALITY_AGENT_API_KEY # 质量评分
MYSQL_HOST/PORT/USER/PASSWORD/DATABASE  # 爬虫数据库
COZE_JWT_TOKEN       # 配图生成

# CMS 发布（部署时加）
CMS_API_URL          # CMS API 地址
CMS_API_KEY          # 或用 CMS_USERNAME + CMS_PASSWORD
```

## 吞吐量

- 纯评分+Quality: ~100 篇/小时
- 含改写完整链路: ~70 篇/小时
- 默认每轮取 50 篇

## 容错

| 故障点 | 处理 |
|---|---|
| DeepSeek 断连 | 该批次跳过，标记 scoring_error |
| 单篇文章异常 | try/except 跳过，记录 rewrite_error |
| 整轮崩溃 | 外层 scheduler 接住，下轮继续 |
| tracker 膨胀 | 超 50000 条自动裁剪 |
| CMS 发布失败 | 记录 publish_blocked，不阻塞 |

## 文件结构

```
agents/          # 各 Agent 实现
  scoring_agent/  # AI 评分 (DeepSeek)
  quality_agent/  # 写作质量评分
  research_agent/ # 调研
  writer_agent/   # 写作/改写
  editor_agent/   # 编辑/审校
  seo_agent/      # SEO 关键词
  image_agent/    # Coze 配图
  cms_agent/      # CMS 发布
scripts/         # 运行入口
  run_scheduler.py   # 生产调度器
  run_production.py  # 生产 Pipeline
  run_test_schedule.py # 测试调度器
  pipeline_batch.py    # SQL 解析
output/          # 运行输出
  production/    # 生产结果
  test_schedule/ # 测试结果
  article_tracker.json # 打分追踪
config/          # 品牌/关键词配置
sql/             # 数据库迁移
```

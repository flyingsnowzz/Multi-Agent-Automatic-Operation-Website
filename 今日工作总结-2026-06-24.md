# 今日工作总结 - 2026-06-24

## 一、今天完成的核心目标

今天主要把内容生产链路从“只判断素材值不值得做”，推进到了“判断素材价值、判断文章质量、生成改写提示词、调用 WriterAgent 生成文章、再用 QualityAgent 复评”的闭环。

当前主流程如下：

```text
文章评分 Agent
  ↓ article_score > 75
QualityAgent 首次评估原文质量
  ↓ 原文 quality_score < 70
ResearchAgent 生成大纲和 WriterAgent prompt
  ↓
WriterAgent 生成文章
  ↓
QualityAgent 复评生成稿
  ↓ quality_score < 85 时继续重写，最多 5 次
  ↓
quality_score >= 85 进入后续发布候选
```

## 二、文章评分 Agent 调整

文章评分 Agent 现在只负责判断文章素材本身是否值得做，不再评价字数和写作质量。

保留维度：

| 维度 | 权重 | 来源 |
| --- | ---: | --- |
| 标题价值 | 25% | 大模型 |
| 是否通知 | 5% | 大模型/规则判断 |
| 内容重要性 | 60% | 大模型阅读全文判断 |
| 时效性 | 10% | 代码规则 |

删除或转移：

- 字数评分已从文章评分 Agent 移到 QualityAgent。
- topic 排名、topic frequency、topic match、AI 推荐度、档位、balance bonus 等旧逻辑不再作为当前主流程。

## 三、QualityAgent 新增

QualityAgent 用来评价文章写得好不好，而不是事件本身重不重要。

首次评估 crawler 原文时：

| 维度 | 权重 | 来源 |
| --- | ---: | --- |
| 字数 | 20% | 代码 |
| 流畅度 | 25% | 大模型 |
| 结构 | 20% | 大模型 |
| 吸引力 | 25% | 大模型 |
| AI 味 | 10% | 大模型 |

评估 WriterAgent 生成稿时：

| 维度 | 权重 | 来源 |
| --- | ---: | --- |
| 字数 | 10% | 代码 |
| 流畅度 | 20% | 大模型 |
| 结构 | 20% | 大模型 |
| 吸引力 | 20% | 大模型 |
| AI 味 | 30% | 大模型 |

这里的 AI 味不是判断真实来源，而是判断“普通人粗看时是否会觉得这像 AI 写的”。生成稿会设置 `if_ai_generated=true`，因此 AI 味权重更高。

## 四、ResearchAgent 和 WriterAgent 调整

ResearchAgent 现在会读取 QualityAgent 的扣分反馈，并把它写进 WriterAgent prompt。

重点包括：

- 如果标题分低，要求 WriterAgent 大改标题，并输出多个标题备选。
- 如果字数不合格，要求非通知类文章控制在 900-1200 字，目标约 1100 字。
- 如果 QualityAgent 指出结构、流畅度、吸引力或 AI 味问题，ResearchAgent 会把这些扣分点作为重写硬约束。
- WriterAgent 生成后会写回 `writer_article_outputs`。
- QualityAgent 会再对生成稿写回 `article_quality_scores`。

新增循环规则：

```text
WriterAgent 生成稿 quality_score < 85
  → 继续重写
  → 最多 5 次
  → 达到 85 即停止
```

## 五、数据库工作

当前新增或重点使用的库表：

| 表 | 作用 |
| --- | --- |
| `research_article_data.research_article_candidates` | 存放进入 ResearchAgent/WriterAgent 的候选文章 |
| `research_article_data.writer_article_outputs` | 存放 WriterAgent 生成文章 |
| `research_article_data.article_quality_scores` | 存放原文和生成稿的质量评分 |

`article_quality_scores.source_kind` 当前支持：

| source_kind | 含义 |
| --- | --- |
| `original` | crawler 原文质量评分 |
| `writer` | WriterAgent 生成稿，按 `if_ai_generated=true` 严格评估 AI 味 |
| `writer_plain` | 同一篇生成稿按 `if_ai_generated=false` 复评，用来观察纯质量差异 |

## 六、WriterAgent 改写效果统计

统计口径：

- 原文不达标：`source_kind='original'` 且 `quality_score < 70`
- 对比对象：同一个 `candidate_id` 已存在 `source_kind='writer'` 的生成稿评分
- 样本数：34 篇

### 总体统计

| 指标 | 原文质量分 | Writer 后质量分 | 提升值 |
| --- | ---: | ---: | ---: |
| 样本数 | 34 | 34 | 34 |
| 平均值 | 62.00 | 81.03 | +19.04 |
| 中位数 | 63.50 | 81.87 | +17.87 |
| 标准差 | 9.49 | 3.01 | 9.64 |
| 最低值 | 16.00 | 74.75 | +6.18 |
| 最高值 | 69.82 | 86.60 | +63.00 |
| Q1 | 62.19 | 80.40 | +16.00 |
| Q3 | 66.00 | 82.00 | +19.40 |

### 通过情况

| 结果 | 数量 |
| --- | ---: |
| Writer 后分数提升 | 34 |
| Writer 后分数下降 | 0 |
| Writer 后达到 85 | 4 |
| Writer 后仍需人工审核/继续优化 | 30 |

### 原文质量分直方图（bin=5）

```text
15-20   1 █
40-45   1 █
55-60   4 ████
60-65  15 ███████████████
65-70  13 █████████████
```

### Writer 后质量分直方图（bin=5）

```text
70-75   1 █
75-80   7 ███████
80-85  22 ██████████████████████
85-90   4 ████
```

### 提升值直方图（bin=5）

```text
 5-10   1 █
10-15   7 ███████
15-20  19 ███████████████████
20-25   4 ████
25-30   1 █
40-45   1 █
60-65   1 █
```

### 观察

WriterAgent 对原本低质量文章的整体提升比较稳定：34 篇全部提升，平均提升 19.04 分。大多数生成稿集中在 80-85 分区间，说明文章整体质量已明显高于原文，但还没有大量稳定跨过 85。

目前主要瓶颈不是“是否能写得更完整”，而是“AI 味”和“吸引力”还会压分。尤其是生成稿按 `if_ai_generated=true` 评分时，AI 味权重为 30%，会明显拉低最终分。

## 七、典型对照：if_ai_generated 的影响

同一篇 WriterAgent 生成稿，分别以 `writer` 和 `writer_plain` 评估：

| source_kind | quality_score | 字数 | 流畅度 | 结构 | 吸引力 | AI 概率 | AI 味分 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `writer` | 74.75 | 87.50 | 85.00 | 80.00 | 75.00 | 40.00 | 60.00 |
| `writer_plain` | 83.75 | 87.50 | 85.00 | 80.00 | 82.00 | 15.00 | 85.00 |

这说明正文纯质量已经接近 84，但作为生成稿时会被 AI 味严格扣分。这个结果符合当前质量门槛设计：WriterAgent 不能只做到“文章完整”，还要尽量不像模板化 AI 稿。

## 八、代码和文件清理

今天清理和整理了以下内容：

- 将文档编号调整为：
  - `06-文章评分Agent说明.md`
  - `07-QualityAgent说明.md`
  - `08-ResearchAgent说明.md`
  - `09-WriterAgent说明.md`
- 旧的 `07-ResearchAgent说明.md` 和 `08-WriterAgent说明.md` 已被新编号替代。
- 新建表和写回工具中不再保留旧的 `length_score/article_length_score` 字段，字数评分统一由 QualityAgent 的 `word_count_score` 负责。
- 没有保留临时 histogram、csv、json 等测试输出文件。
- dummy 数据测试文件属于正式单元测试，暂时保留。

## 九、验证情况

已完成本地验证：

```text
44 个单元测试通过
py_compile 通过
git diff --check 通过
```

测试覆盖的重点包括：

- 文章评分 Agent 删除字数分后的输出结构。
- QualityAgent 字数、流畅度、结构、吸引力、AI 味评分。
- ResearchAgent 接收 QualityAgent 扣分反馈。
- WriterAgent 生成稿写回数据库。
- WriterAgent 低于 85 最多重写 5 次的闭环逻辑。

## 十、下一步建议

下一步重点应该是优化 WriterAgent prompt，而不是继续大幅改评分规则。

建议顺序：

1. 抽取 5-10 篇 Writer 后仍在 80-85 分的文章，人工阅读，看扣分主要来自 AI 味、吸引力还是结构。
2. 针对 AI 味继续优化 WriterAgent prompt：减少工整转场、减少模板化段落、增加具体细节和非对称表达。
3. 对 75-85 分区间文章做二次重写样本测试，观察是否能把稳定均分从 81 推到 85 附近。
4. 等质量分稳定后，再接 EditorAgent 或发布候选库。
